spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/loja_automatica
    username: postgres
    password: 247935
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true
logging:
  level:
    br.uesb.cipec: DEBUG
spring-doc:
  paths-to-match: /api/**
  swagger-ui:
    use-root-path: true

server:
  port: 8080

spring.mail.host: smtp.gmail.com
spring.mail.port: 587
spring.mail.username: loja.peticomputacao@gmail.com 
spring.mail.password: uncxecldibjxvsbf
spring.mail.properties.mail.smtp.auth: true
spring.mail.properties.mail.smtp.starttls.enable: true